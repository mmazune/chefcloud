# Railway Environment Variables Template
# Copy this file and fill in the values for your Railway services

# ===========================================
# API Service (chefcloud-api)
# ===========================================

# Database (from Railway PostgreSQL service)
DATABASE_URL=postgresql://postgres:password@host:port/database

# Node Environment
NODE_ENV=production

# JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET=your-secure-random-string-here

# Demo Protection
DEMO_PROTECT_WRITES=1
DEMO_TAPAS_ORG_SLUG=tapas-demo

# API Port (Railway assigns automatically)
PORT=3333

# CORS (update after Vercel deployment)
CORS_ORIGIN=https://your-app.vercel.app

# Email/SMTP (optional)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=SG.your-sendgrid-api-key
EMAIL_FROM=noreply@chefcloud.app

# Observability (optional)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
OTEL_EXPORTER_OTLP_ENDPOINT=https://your-otlp-endpoint

# Redis (optional, for rate limiting)
REDIS_URL=redis://user:password@host:port

# Security - DO NOT SET THESE IN PRODUCTION
# E2E_ADMIN_BYPASS=  # NEVER SET IN PRODUCTION
# E2E_AUTH_BYPASS=   # NEVER SET IN PRODUCTION

# ===========================================
# Worker Service (chefcloud-worker)
# ===========================================

# Database (same as API)
DATABASE_URL=postgresql://postgres:password@host:port/database

# Node Environment
NODE_ENV=production

# Redis (for BullMQ job queues)
REDIS_URL=redis://user:password@host:port

# Email (same as API)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=SG.your-sendgrid-api-key
EMAIL_FROM=noreply@chefcloud.app

# Observability (optional)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
