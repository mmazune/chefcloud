# Nimbus POS / ChefCloud — Session State

date: 2026-01-10
repo: mmazune/nimbuspos
branch: main
head_sha: d8747d3ef4359ee9fd8ee72d357fba969154f619
objective: "Phase C — Deep cleanup (quarantine/remove dormant + incomplete features)"

definition_of_done:
  - DORMANT_FEATURE_REGISTRY.md created with evidence
  - QUARANTINE_RULES.md documented
  - PHASE_C_PLAN.md created with subphase DoDs
  - High-confidence deletions proven safe and removed
  - WIP features quarantined with zero production imports
  - Gates pass (lint, build)
  - Atomic commits per subphase

gates_required:
  - pnpm -C services/api lint
  - pnpm -C apps/web lint
  - pnpm -C services/api build
  - pnpm -C apps/web build

notes: |
  Phase C is cleanup-focused.
  C1 = registry only (no deletions).
  C2 = high-confidence deletions (proven unreferenced).
  C3 = quarantine WIP (zero production imports).
  C4 = medium-confidence improvements (optional).
  DO NOT break existing functionality.
