# Nimbus POS / ChefCloud — Session State

date: 2026-01-10
repo: mmazune/nimbuspos
branch: main
head_sha: 60509b70f3746af5d50403edd422a567dc29e52c
objective: "Phase C3.1 — Quarantine DevPortal disabled variant + document MSR/SmartSprout"

definition_of_done:
  - DevPortal disabled variant moved to wip/dev-portal/api-module/
  - MSR Login documented as ACTIVE (not WIP)
  - Smart Sprout documented as PLANNED (no code)
  - Test imports fixed to use stubs (not quarantine paths)
  - Zero production imports from wip/
  - Gates pass (lint, build)
  - Atomic commit with proof

gates_required:
  - pnpm -C services/api lint
  - pnpm -C apps/web lint
  - pnpm -C services/api build
  - pnpm -C apps/web build

notes: |
  Phase C is cleanup-focused.
  C1 = registry only (no deletions).
  C2 = high-confidence deletions (proven unreferenced).
  C3 = quarantine WIP (zero production imports).
  C4 = medium-confidence improvements (optional).
  DO NOT break existing functionality.
