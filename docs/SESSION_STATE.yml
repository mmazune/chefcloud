# Nimbus POS / ChefCloud — Session State

date: 2026-01-10
repo: mmazune/nimbuspos
branch: main
head_sha: 3228f4d1c4131dc1528e1e539d76434b16f963a6
objective: "Phase B — Codebase mapping (routes, API, schema, roles, seeds)"

definition_of_done:
  - Frontend route map generated (JSON + markdown)
  - Backend API map generated (JSON + markdown)
  - Schema domain map generated (JSON + markdown)
  - Role access model documented
  - Sample data/seeds documented
  - Orphans report generated
  - Gates pass (lint, build)
  - Single atomic commit

gates_required:
  - pnpm -C services/api lint
  - pnpm -C apps/web lint
  - pnpm -C services/api build
  - pnpm -C apps/web build

notes: |
  Phase B is mapping-only.
  DO NOT implement features.
  DO NOT change runtime behavior.
  DO NOT delete code (log candidates only).
